<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/action_save_log"
        android:icon="@drawable/ic_action_save_log"
        android:orderInCategory="90"
        android:title="@string/text_action_save_log"
        android:visible="false"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/action_empty_log"
        android:icon="@drawable/ic_action_empty_log"
        android:orderInCategory="100"
        android:title="@string/text_vaciar_log"
        android:visible="false"
        app:showAsAction="never" />

</menu>
